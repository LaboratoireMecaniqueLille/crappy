set HTTPS_PROXY=http://yourproxyadress.com:port or add it to your environnement variable

pip install numpy

pip raises an error about lapack/blas when installing scipy and scikit-image cannot be installed with pip using Windows.
Download scipy and scikit-image python wheel and then:
pip install *package-name*.whl
You can get both .whl files from this address: 
http://www.lfd.uci.edu/~gohlke/pythonlibs/ (make sure to select the correct version)

pip install matplotlib
pip install SimpleITK
pip install pyserial
pip install pandas

Download latest OpenCV release for Windows from sourceforge site and double-click to extract it.
Goto opencv/build/python/2.7 folder.
Copy cv2.pyd to C:/Python27/lib/site-packeges.

To compile C++ modules, you need to install Visual C++ For Python2.7.

After installing Ximea driver, you may need to add c:\\XIMEA\\API\x64 to your PATH environnement variable.

- if you want ipython:
	
pip install pyreadline
pip isntall ipython